# 宣传视频实施状态

更新日期：2026-08-11

## 已完成

- 建立 `promo-video/` 非破坏性制作工程；
- 建立 105 秒、15 镜头的机器可读时间轴；
- 为 5 个当前使用的源视频建立 SHA-256 素材清单；
- 生成 1080 × 1920、30 fps 代理文件；
- 生成 Part 01、02、03、05、06 接触表；
- 根据接触表修正候选、现场核对、地图和等待协助的选段；
- 渲染 15 个独立场景片段；
- 拼接 1920 × 1080、30 fps、105 秒结构粗剪；
- 使用本地 Microsoft Huihui Desktop 生成不产生云费用的临时旁白；
- 生成 15 段 SRT 中文字幕；
- 封装带临时旁白和 `zho` 字幕轨的审片版；
- 生成真实自然原声的波形和频谱底稿；
- 使用项目内 OpenStreetMap 杭州底图，以 Python/Pillow + FFmpeg 代码生成 25 秒宽银幕地图动画；
- 地图动画包含程序化镜头运动、色彩分级、区域点位、声源涟漪、连线、动态计数和音频波形；
- 已用代码地图动画替换 S10–S12，并重新封装带临时旁白的审片版。
- 完成地图 v002 细节精修：修正“坐标”文字、消除点位随镜头漂移、补充低透明度网格与扫描、路径流动粒子、演示标识和协助监听状态；
- 输出 `xykw-promo-rough-vo-v002.mp4` 作为当前推荐审片版。
- 输出 v003：补齐 S01、S03、S06、S07、S13、S15 动态画面，15 个镜头不再含视觉占位卡；
- 生成 MiniMax `speech-2.8-hd` 正式中文旁白，并完成音乐自动避让、自然原声点缀与 -16 LUFS 目标混音；
- 输出 `xykw-promo-polish-v003.mp4`，作为当前推荐精修审片版。
- 输出 v004：生成式杭州晨雾悬念开场、录屏编辑侧栏、隐私化地图 v003、S14 声音明信片高潮与设计字幕；
- 输出 `xykw-promo-polish-v004.mp4` 及无字幕母版 `xykw-promo-polish-v004-clean.mp4`，v004 为当前推荐审片版。

## 当前媒体检查

| 项目 | 结果 |
| --- | --- |
| 审片分辨率 | 1920 × 1080 |
| 帧率 | 30 fps |
| 视频编码 | H.264 |
| 音频编码 | AAC，48 kHz，双声道 |
| 字幕 | ASS 设计字幕烧录；阿里巴巴普惠体 |
| 审片时长 | 105.00 秒 |
| 综合响度 | -16.0 LUFS |
| True Peak | -3.2 dBFS |
| 临时旁白平均音量 | -28.0 dB |
| 临时旁白峰值 | -3.6 dB |
| 字幕段数 | 15 |

临时旁白只用于判断节奏，不能作为正式交付音轨。正式旁白需要重新生成或录制，并完成响度标准化、去齿音、均衡和最终混音。

## 当前占位镜头

- S01：独立生成的“声音悬案”开场；
- S03：鸟、蛙、虫、流水、雨和风的多类自然声蒙太奇；
- S07：多模型与规则融合技术流程；
- S15：定制品牌字标和片尾动画。

## 后续可选优化

1. 如获得更多实拍素材，用蛙声、流水和雨景镜头替换 S03 中偏海报化的部分；
2. 按发布渠道制作 9:16 重排版，而非直接裁切横屏母版；
3. 发布前确认平台对 AI 生成内容标识的位置与尺寸要求。
