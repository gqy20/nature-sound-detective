# 多类群声学识别接入

## 当前交付边界

- 鸟类：BirdNET 2.4 使用杭州全年地理先验筛选的 200 种候选，服务端和 Flutter 共用 `mobile/assets/labels/birdnet_hz.json`，不是原来的 6 种白名单。
- 蛙类与鸣虫：训练、评测、TFLite 导出和端云推理链路已经接入。当前仓库安装了 5 类 `source_curated` 基线；它按来源标签训练并只输出候选，不代表已经通过杭州公园人工听审验收。
- 声景大类：Qwen/YAMNet 继续负责鸟、蛙、虫、流水、风、人声和交通等多标签场景；专业分类器只增强物种候选。

## 运行链路

录音上传后生成两份单声道 PCM：48 kHz 生物声学主文件供 BirdNET 与蛙虫分类头使用，16 kHz 副本供通用声音模型使用。这样不会在上传阶段丢掉蛙虫常见的高频线索。

BirdNET 每 3 秒产生 6,522 个鸟种分数和一个 1,024 维嵌入。鸟类分支仅保留杭州 200 种目录；非鸟分支复用同一嵌入，经过独立多标签 MLP 分类头输出蛙类、鸣虫和背景类别。融合层保留混合声景中的多个类别，并把具体物种统一放入 `detections[].specific_species`。

## 数据和训练

1. 使用采集脚本生成统一候选清单，或复制 `data/metadata/curated_nonbird_sources_template.csv` 接入国内博物馆、专题库与研究团队授权数据。
2. 下载候选音频并在本地复核页面逐条确认。脚本不会把 `pending`、未知许可、禁止演绎或缺少本地音频的记录送入训练。
3. 从已审核候选生成 `data/metadata/nonbird_training_manifest.csv`，其中包含真实音频路径、类群标签、来源、许可证、复核状态和分组 ID。
4. 只允许 `human_reviewed`、`expert_confirmed` 或 `approved` 样本进入正式训练；同一原始录音或同一采集组不能跨训练、验证和测试集，避免切片泄漏。
5. MVP 优先训练黑蚱蝉；布氏泛树蛙以及 `other_insect`、`other_frog`、`background` 用于扩展与拒识。类别定义以 `ml/configs/hangzhou_nonbird_species.json` 为唯一来源。

```powershell
# 中国范围内的 iNaturalist 研究级声音元数据；加 --download 才会下载音频
uv run python scripts/collect_inaturalist_sounds.py

# InsectSet459 默认筛选 Cicadidae，每个物种先取 20 条候选
uv run python scripts/select_insectset459.py

# 国内人工整理来源：只有填写直接媒体地址且许可可用的行会被接受
uv run python scripts/import_curated_bioacoustics.py data/metadata/curated_nonbird_sources_template.csv --download

# 浏览器打开 http://127.0.0.1:8766 完成人工听审
uv run python scripts/review_nonbird_candidates.py data/metadata/inaturalist_nonbird_candidates.csv

# 审计并生成正式训练清单
uv run python scripts/audit_nonbird_candidates.py data/metadata/inaturalist_nonbird_candidates.csv --require-ready
uv run python scripts/build_nonbird_training_manifest.py data/metadata/inaturalist_nonbird_candidates.csv data/metadata/curated_nonbird_candidates.csv

# 将 m4a/mp3 统一成 BirdNET 可读取的 48 kHz 单声道 WAV
uv run python scripts/prepare_nonbird_training_audio.py data/metadata/nonbird_source_curated_training_manifest.csv

uv run python scripts/extract_nonbird_embeddings.py data/metadata/nonbird_source_curated_prepared_manifest.csv --output artifacts/nonbird/source_curated_embeddings.npz
uv run python scripts/train_nonbird_classifier.py artifacts/nonbird/source_curated_embeddings.npz --output-dir artifacts/nonbird/source_curated_model
uv run python scripts/evaluate_nonbird_classifier.py artifacts/nonbird/source_curated_embeddings.npz artifacts/nonbird/source_curated_model
uv run python scripts/export_nonbird_tflite.py artifacts/nonbird/source_curated_model --output-dir artifacts/nonbird/source_curated_export --install-mobile --install-server
uv run python scripts/validate_bioacoustic_models.py

# 使用测试集目标声与同集合背景生成可重复的噪声、混响和手机频响压力集
uv run python scripts/build_nonbird_stress_set.py data/metadata/nonbird_source_curated_prepared_manifest.csv
uv run python scripts/extract_nonbird_embeddings.py artifacts/nonbird/stress_manifest.csv --output artifacts/nonbird/stress_embeddings.npz
uv run python scripts/evaluate_nonbird_robustness.py artifacts/nonbird/stress_embeddings.npz artifacts/nonbird/source_curated_model

# 用验证集校准精确率优先阈值、背景门控、嵌入距离与连续窗口规则
uv run python scripts/calibrate_nonbird_rejection.py artifacts/nonbird/source_curated_embeddings.npz artifacts/nonbird/source_curated_model

# 将服务端反馈目录或 Flutter 导出的 feedback.jsonl 转成人工复核队列
uv run python scripts/build_feedback_review_manifest.py outputs/mvp/feedback mobile-review/feedback.jsonl
uv run python scripts/review_nonbird_candidates.py data/metadata/nonbird_feedback_review_candidates.csv

# 仅追加 human_reviewed 反馈，固定旧测试集进行干净集和压力集回归；默认不安装
uv run python scripts/run_nonbird_training_cycle.py
# 候选通过具体物种精确率、F1 退化和背景误报三类门槛后才允许覆盖当前模型
uv run python scripts/run_nonbird_training_cycle.py --run-id park-feedback-v1 --version 0.3.0 --install
```

最后一条命令同时检查 BirdNET 文件哈希、`[1, 144000]` 输入、`[1, 6522]` 鸟种输出、`[1, 1024]` 嵌入输出、200 种目录，以及已安装非鸟模型的输入输出和类别配置。

截至 2026-08-03，仓库保存的 iNaturalist 中国候选快照包含 60 条许可可用的研究级声音：黑蚱蝉 44 条、布氏泛树蛙 16 条；其中 CC-BY 14 条、CC0 4 条、CC-BY-NC 41 条、CC-BY-NC-SA 1 条。本地工作区已经下载全部 60 条音频并记录 SHA-256；其中发现 1 条同源重复，已隔离为 `duplicate`，实际有 59 份唯一音频。音频本身受 `.gitignore` 保护，不进入仓库。其余记录保持 `pending`，尚未被人工确认，因此当前可训练条数仍为 0。

### 来源标签训练版本

根据当前交付选择，另行生成了 `source_curated` 数据版本，直接信任 iNaturalist 的研究级群体鉴定以及 Freesound 的来源查询标签，但不把它表述为人工听审数据。该版本共放行 283 条记录：黑蚱蝉 43、布氏泛树蛙 16、其他蝉科 94、其他蛙类 100、背景 30；训练/验证/测试分别为 196/38/49，五个类别在三个集合中均有正样本。同一观察记录固定在同一集合，原始文件哈希重复项已隔离。

来源标签适合快速训练可运行基线，但其评测衡量的是对来源标签的拟合能力，不等同于杭州公园现场准确率。后续加入公园实录时，应保留一份人工确认的杭州独立测试集。

该来源标签版本已经完成训练并安装到服务端与 Flutter。283 个文件统一截取到最长 30 秒后共约 89.8 分钟，生成 1,899 个 BirdNET 嵌入窗口。独立来源测试集包含 311 个窗口，F1 分别为：黑蚱蝉 0.954、布氏泛树蛙 0.909、其他鸣虫 0.895、其他蛙类 0.832、背景 0.933。完整机器可读报告位于 `data/metadata/nonbird_source_curated_model_report.json`。

同一测试集还生成了 371 条压力录音、2,278 个嵌入窗口。原始模型在纯背景上的窗口误报率为 13.33%；信噪比降到 -5 dB 时，黑蚱蝉、布氏泛树蛙、其他鸣虫、其他蛙类 F1 分别降至 0.772、0.556、0.805、0.548。摘要保存在 `data/metadata/nonbird_source_curated_stress_report.json`，派生音频与完整缓存留在本地 `artifacts/` 和 `data/interim/`。

0.2.0 运行策略在服务端和 Flutter 采用相同的四层拒识：精确率优先类别阈值、背景概率门控、训练嵌入中心余弦距离、连续窗口支持。校准后纯背景窗口误报率从 13.33% 降至 3.33%；严重噪声时系统优先放弃判断，而不是强行输出物种。

反馈默认只保存“像 / 不像 / 不确定”和更正标签，不复制录音。只有用户明确勾选同意后，服务端才把任务音频复制到反馈审核目录；Flutter 也只会把同意留存的记录写入审核导出包。所有反馈先进入 `user_reported/pending` 队列，必须经复核工具确认成 `human_reviewed` 后才能进入训练。

增量训练只把反馈样本追加到 `train`，不会改变既有验证/测试录音。每轮产物保存在独立的 `artifacts/nonbird/cycles/<run-id>/`，默认只生成候选；模型必须满足具体物种 precision ≥ 0.90、任一非背景类别 F1 相对固定基线下降不超过 0.08、纯背景窗口误报率 ≤ 0.05，且显式传入 `--install` 才能安装。

## 启用门槛

非鸟模型至少要通过独立测试集的逐类召回、精确率和误报检查，并在目标公园的风、雨、人声、交通、鸟鸣混响背景下复测。产品显示沿用“候选”口径，并允许用户反馈；未达门槛的类别留在大类识别，不显示具体物种。

BirdNET 模型采用 CC-BY-NC-4.0，当前适合非商业比赛与研究演示。任何商业发布前都需要重新确认模型、训练数据和声音素材的许可证。
