# Community narrative v017 review

- Scope: S10–S12 only, 1080p debug renders; the full film was not rendered.
- S10: removed the `演示数据` badge while preserving the coded Hangzhou map and geographic base.
- S11: the real product screen stays on the left; the right side now visualizes three city sound nodes and their collective meaning instead of enlarging the map.
- S12: the real assistance screen stays on the left; the right side now visualizes an investigator-to-investigator relay instead of enlarging the same task card.
- Decorative waveforms were removed from the explanatory side. The relay is represented by a line and moving data points, so it cannot be mistaken for waveform data derived from audible sound.
- Review extraction: 2 fps — S10 12 frames, S11 18 frames, S12 20 frames.
- Output: 1920×1080, 30 fps, synchronized excerpts from `final-mix-v011.wav`.
